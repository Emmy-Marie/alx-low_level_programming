#include "main.h"

/**
 * _islower - Entry point
 * @c - The integer value
 * Return:1 - if c is lower case
 * Return:0 - if otherwise
 */

int _islower(int c)
{
	int r = r + '0';

	if (r < 'a' || r > 'z')
	{
		_putchar('0');
	}
	else
	{
		_putchar('1');
	}
}
