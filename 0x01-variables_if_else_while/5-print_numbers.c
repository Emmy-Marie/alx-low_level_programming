#include <stdio.h>

/**
 * main - A program that prints all single bas 10 digit numbers
 * Return:0 (success)
 */

int main(void)
{
	int i = 0;

	while (i < 10)
	{
		printf("%d", i);
		i++;
	}
	putchar('\n');
	return (0);
}
