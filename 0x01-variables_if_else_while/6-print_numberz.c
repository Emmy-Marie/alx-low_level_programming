#include <stdio.h>

/**
 * main - print single base 10 digits using putchar function
 * Return:0 (success)
 */

int main(void)
{
	int i, c;

	c = i + '0'
	while (c <= '9')
	{
		putchar(c);
		c++;
	}
	putchar('\n');
	return (0);
}
