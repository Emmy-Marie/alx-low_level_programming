#include <stdio.h>

/**
 * main - A program that prints a string using puts() function
 * Return:0 (The programme runs a success)
 */

int main(void)
{
	char string_[] = "\"Programming is like building a multilingual puzzle";

	puts(string_out);

	return (0);
}

